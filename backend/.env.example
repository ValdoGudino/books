# Copy to .env and fill in. Never commit .env.
# With direnv: cd backend, run `direnv allow`, then vars load automatically.

GOOGLE_BOOKS_API_KEY=

# Local MongoDB (e.g. docker compose up -d). Leave unset to skip cache; integration tests do not set this.
MONGODB_URI=mongodb://localhost:27017
