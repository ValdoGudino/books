# Secret config. Copy to .env.secrets and fill in. Never commit .env.secrets.
# Loaded automatically by direnv (see .envrc).

# Google Books API key — optional but recommended to avoid anonymous quota limits.
GOOGLE_BOOKS_API_KEY=
