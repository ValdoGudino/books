# Non-secret config. Copy to .env.local and edit.
# Loaded automatically by direnv (see .envrc).

# Local MongoDB (e.g. docker compose up -d). Leave unset to skip cache.
MONGODB_URI=mongodb://localhost:27017

# Timezone for "today" and stats (e.g. America/Chicago for Central Time). Default: UTC.
# APP_TIMEZONE=America/Chicago
